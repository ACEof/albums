<head>
  <link rel="stylesheet" href="/src/css/album.css">
  <title>Albums</title>
</head>
<body>
  <h1>Albums</h1>
  <a href="/logout" id="logout">LogOut</a> <br>
  <p id="userName">{{name}}</p>

  <div>
      <form action="/add-albums" method="POST">
          <input type="text" name="title">
          <input type="submit" value="Add album" class="button">
      </form>
  </div>

  <div>
      <form action="/delete-album" method="POST">
          <input type="text" name="albumTitle">
          <input type="submit" class="button" value="Delete Album">
      </form>
  </div>

  <div>
      <form action="/rename" method="POST">
          <label>Old Title</label> <input name="oldTitle" type="text"> <label>New Title</label> <input name="newTitle" type="text"> <input type="submit" class="button" value="Rename">
      </form>
  </div>

  <div id="listAlbum">
      <ul id="titleList">
        {{#each title}}
            <li> <a href="show-album-{{this}}" class="listAlbum">{{this}}</a></li>
        {{/each}}
      </ul>
  </div>
</body>